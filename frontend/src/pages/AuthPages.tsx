import {FormEvent,useState} from 'react';import {Link,useNavigate} from 'react-router-dom';import {ArrowLeft,CheckCircle2,Eye,Factory,Gauge,LockKeyhole,ShieldCheck,UserRound} from 'lucide-react';import {authApi} from '../api/authApi';
export function LoginPage(){const nav=useNavigate(),[error,setError]=useState(''),[busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);setBusy(true);try{const user=await authApi.login(String(f.get('username')),String(f.get('password')));localStorage.setItem('prima-auth','true');localStorage.setItem('prima-user',JSON.stringify(user));nav('/dashboard')}catch(e){setError(e instanceof Error?e.message:'로그인 실패')}finally{setBusy(false)}}return <div className="auth-page"><section className="auth-hero"><div className="hero-grid"/><div className="auth-logo"><Gauge/><span><b>PRIMA</b> FACTORY 360</span></div><div className="hero-copy"><span className="eyebrow light">SMART FACTORY MONITORING</span><h1>공장의 오늘을<br/><em>10초 안에</em> 판단하세요.</h1><p>프레스와 자동화 로봇의 생산·정지·손실을<br/>한 화면에서 실시간으로 확인합니다.</p><div className="hero-points"><span><Factory/> 3개 생산라인 통합</span><span><ShieldCheck/> 설비 데이터 READ 전용</span></div></div><small>외부 접속은 회사 VPN을 이용해 주세요.</small></section><section className="auth-form-wrap"><form className="auth-form" onSubmit={submit}><div className="mobile-auth-logo"><Gauge/> PRIMA FACTORY 360</div><span className="eyebrow">WELCOME BACK</span><h2>안녕하세요,</h2><p>통합 모니터링 시스템에 로그인하세요.</p>{error&&<div className="form-error">{error}</div>}<label>아이디<div className="input-icon"><UserRound/><input name="username" placeholder="아이디를 입력하세요"/></div></label><label>비밀번호<div className="input-icon"><LockKeyhole/><input name="password" type="password" placeholder="비밀번호를 입력하세요"/><Eye/></div></label><div className="form-row"><label className="check"><input type="checkbox" defaultChecked/> 로그인 유지</label><button type="button" className="link-btn">비밀번호 찾기</button></div><button className="primary-btn" disabled={busy}>{busy?'로그인 확인 중...':'로그인'}</button><p className="join">계정이 없으신가요? <Link to="/signup">가입 승인 요청</Link></p><div className="security-note"><ShieldCheck/><span><b>안전한 모니터링 전용 시스템</b>원격 기동·정지 및 PLC WRITE 기능을 제공하지 않습니다.</span></div></form></section></div>}
export function SignupPage(){const nav=useNavigate(),[done,setDone]=useState(false),[error,setError]=useState(''),[busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);setError('');setBusy(true);try{await authApi.register(Object.fromEntries(f) as never);setDone(true)}catch(e){setError(e instanceof Error?e.message:'가입 신청 저장에 실패했습니다.')}finally{setBusy(false)}}if(done)return <div className="center-page"><div className="success-card"><CheckCircle2/><h1>가입 신청이 완료되었습니다</h1><p>관리자 승인 후 로그인할 수 있습니다.<br/>승인 결과는 등록한 이메일로 안내됩니다.</p><button className="primary-btn" onClick={()=>nav('/login')}>로그인으로 돌아가기</button></div></div>;return <div className="signup-page"><div className="signup-head"><Link to="/login"><ArrowLeft/></Link><div className="auth-logo dark"><Gauge/><span><b>PRIMA</b> FACTORY 360</span></div><span>관리자 승인형 계정 신청</span></div><form className="signup-form" onSubmit={submit}><span className="eyebrow">CREATE ACCOUNT</span><h1>회원가입</h1><p>정확한 정보를 입력해 주세요. 관리자가 역할과 권한을 검토합니다.</p>{error&&<div className="form-error">{error}</div>}<div className="form-grid">{[['username','사용자 아이디','영문, 숫자 조합'],['name','이름','이름 입력'],['password','비밀번호','8자 이상'],['passwordConfirm','비밀번호 확인','비밀번호 재입력'],['factory','소속 공장','화성 제1공장'],['department','부서','생산관리팀'],['position','직책','부장'],['phone','연락처','010-0000-0000'],['email','이메일','name@company.com']].map(([n,l,p])=><label key={n}>{l}<input required name={n} type={n.includes('password')?'password':'text'} placeholder={p}/></label>)}<label>신청 권한<select name="requestedRole"><option>일반 유저</option><option>관리자</option></select></label></div><div className="approval-note"><ShieldCheck/><span><b>권한은 관리자 승인 후 부여됩니다.</b>관리자 권한을 신청해도 승인 전에는 사용할 수 없습니다.</span></div><label className="check consent"><input required type="checkbox"/> 개인정보 수집 및 이용에 동의합니다.</label><button className="primary-btn" disabled={busy}>{busy?'가입 신청 저장 중...':'가입 승인 요청'}</button></form></div>}
