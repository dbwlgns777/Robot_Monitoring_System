export type EquipmentStatus='RUNNING'|'WAITING'|'SPEED_LOSS'|'SHORT_STOP'|'LONG_WAIT'|'ROBOT_FAULT'|'PRESS_FAULT'|'MATERIAL_WAIT'|'DOWNSTREAM_WAIT'|'MANUAL'|'TEACHING'|'PLANNED_STOP'|'SAFETY_STOP'|'COMMUNICATION_LOSS'|'UNCLASSIFIED_STOP';
export type CommunicationStatus='CONNECTED'|'DEGRADED'|'DISCONNECTED';export type DataQuality='GOOD'|'UNCERTAIN'|'BAD';export type EquipmentType='ROBOT'|'PRESS';
export interface Equipment{id:string;code:string;name:string;lineId:string;type:EquipmentType;status:EquipmentStatus;operatingStatus?:EquipmentStatus;communicationStatus?:CommunicationStatus;statusDuration:string;product:string;target:number;actual:number;cycleTime:number;alarm?:string;lastReceived:string;responseMs:number;enabled:boolean;manufacturer:string;model:string;ip:string;protocol:string;}
export interface ProductionLine{id:string;name:string;product:string;status:EquipmentStatus;target:number;actual:number;expected:number;uptime:number;equipmentIds:string[];}export interface Kpi{running:number;idle:number;stopped:number;alarms:number;target:number;actual:number;achievement:number;hourly:number;avgCycle:number;downtime:number;estimatedLoss:number;forecast:number;yesterdayDelta:number;weeklyUptimeDelta:number;}export interface HourlyProduction{hour:string;target:number;actual:number;}export interface Alarm{id:string;equipmentCode:string;line:string;code:string;message:string;occurredAt:string;duration:number;count:number;severity:'critical'|'warning';cleared:boolean;}export interface DowntimeReason{reason:string;minutes:number;loss:number;count:number;color:string;}export interface Product{id:string;code:string;name:string;vehicle:string;customer:string;line:string;cycleTime:number;hourlyRate:number;target:number;mold:string;enabled:boolean;}export interface CollectionHealth{equipmentCode:string;state:CommunicationStatus;lastReceived:string;successRate:number;failures:number;responseMs:number;missing:number;reconnects:number;}export interface RegistrationForm{username:string;name:string;password:string;passwordConfirm:string;factory:string;department:string;position:string;phone:string;email:string;requestedRole:string;}export interface SystemHealth{collector:boolean;api:boolean;websocket:boolean;dbUsage:number;diskFree:number;cpu:number;memory:number;lastBackup:string;backupSuccess:boolean;}
export interface RegistrationRequest{id:number;username:string;fullName:string;factoryName:string;department?:string;position?:string;phone?:string;email:string;requestedRole:string;status:'PENDING'|'APPROVED'|'REJECTED';createdAt:string;}
export interface AssignableRole{roleCode:string;roleName:string;}
