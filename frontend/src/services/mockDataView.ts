// Legacy static analysis view data used only when VITE_DATA_SOURCE=mock; API-connected live pages use MonitoringProvider.
export {alarms,daily,downtime,equipment,hourly,collection,products,systemHealth} from '../data/mockData';
