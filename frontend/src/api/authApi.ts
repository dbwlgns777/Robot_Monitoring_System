import type {RegistrationForm} from '../types/domain';import {request} from '../services/apiClient';export const authApi={login:(username:string,password:string)=>request<{name:string;role:string}>('/auth/login',{method:'POST',body:JSON.stringify({username,password})}),register:(form:RegistrationForm)=>request<{status:'PENDING'}>('/auth/signup',{method:'POST',body:JSON.stringify(form)}),logout:()=>request('/auth/logout',{method:'POST'})};
