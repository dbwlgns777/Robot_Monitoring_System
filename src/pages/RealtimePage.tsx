import {useState} from 'react';import {Activity,Clock,Radio,X} from 'lucide-react';import {monitoringApi} from '../api/monitoringApi';import {Card,Loading,PageHeader,Progress} from '../components/Common';import {StatusBadge} from '../components/StatusBadge';import {useMockQuery} from '../hooks/useMockQuery';import type {Equipment} from '../types/domain';
export function RealtimePage(){const {data}=useMockQuery(monitoringApi.getEquipment),[selected,setSelected]=useState<Equipment>();if(!data)return <Loading/>;return <><PageHeader title="공장·라인 실시간 현황" description="설비 상태를 선택하면 상세 운전 정보를 확인할 수 있습니다." actions={<div className="legend"><StatusBadge status="RUNNING"/><StatusBadge status="IDLE"/><StatusBadge status="ALARM"/><StatusBadge status="DISCONNECTED"/></div>}/>{[1,2,3].map(line=><Card key={line} title={`${line}라인 · ${['프론트 패널 LH','사이드 멤버 RH','브래킷 ASSY'][line-1]}`} caption={`로봇 4대 · 프레스 P-0${line}`}><div className="flow-line"><div className="press-node"><b>P-0{line}</b><span>프레스</span><StatusBadge status={line===2?'IDLE':'RUNNING'}/></div>{data.filter(e=>e.lineId===`line-${line}`).map(e=><button className={`equipment-node state-${e.status.toLowerCase()}`} key={e.id} onClick={()=>setSelected(e)}><div><b>{e.code}</b><StatusBadge status={e.status}/></div><strong>{e.name}</strong><span>{e.product}</span><div className="equipment-numbers"><span>실적 <b>{e.actual.toLocaleString()}</b></span><span>CT <b>{e.cycleTime}초</b></span></div>{e.alarm&&<em>{e.alarm}</em>}</button>)}</div></Card>)}{selected&&<div className="drawer-overlay" onClick={()=>setSelected(undefined)}><aside className="detail-drawer" onClick={e=>e.stopPropagation()}><button className="drawer-close" onClick={()=>setSelected(undefined)}><X/></button><span className="eyebrow">EQUIPMENT DETAIL</span><h2>{selected.code} · {selected.name}</h2><StatusBadge status={selected.status}/><div className="detail-hero"><Activity/><div><span>현재 상태 지속시간</span><b>{selected.statusDuration}</b></div></div><h3>실시간 운전 정보</h3><dl className="detail-list"><div><dt>현재 제품</dt><dd>{selected.product}</dd></div><div><dt>생산 실적</dt><dd>{selected.actual.toLocaleString()} / {selected.target.toLocaleString()}개</dd></div><div><dt>최근 사이클타임</dt><dd>{selected.cycleTime}초</dd></div><div><dt>로봇 부하율</dt><dd>64.2%</dd></div><div><dt>서보 상태</dt><dd>ON</dd></div><div><dt>현재 프로그램</dt><dd>MAIN_P{selected.code.slice(2)}</dd></div></dl><h3>통신 상태</h3><div className="comm-card"><Radio/><div><b>{selected.status==='DISCONNECTED'?'연결 끊김':'READ 연결 정상'}</b><span>{selected.ip} · {selected.protocol}</span></div><strong>{selected.responseMs||'-'}ms</strong></div><h3>최근 1시간 사이클</h3><Progress value={82}/><div className="readonly-banner"><Clock/> 조회 전용 화면입니다. 설비 제어 기능은 제공되지 않습니다.</div></aside></div>}</>}
